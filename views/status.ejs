<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>suryapolina.com</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css">
    <link rel="stylesheet" type="text/css" href="/static/styles.css" />
    <link rel="icon" type="image/png" href="/images/favicon.ico">
</head>
<body class="bg-white text-gray-900">
    <!-- Header -->
    <header class="text-center py-6">
        <nav class="mt-4">
            <a href="/about" class="text-blue-600 hover:text-purple-800 mx-2">About</a>
            <a href="/blog" class="text-blue-600 hover:text-purple-800 mx-2">Blog</a>
            <a href="/portfolio" class="text-blue-600 hover:text-purple-800 mx-2">Portfolio</a>
        </nav>
    </header>

    <!-- Main Content -->
    <div class="container mx-auto px-4 py-2">
        <div class="grid max-w-2xl mx-auto">
            <div class="border border-gray-200 p-6 rounded-2xl bg-white">               
                <div class="space-y-6">
                    <% statuses.forEach(function(status) { %>
                        <div class="border-b border-gray-100 last:border-0 pb-6 last:pb-0">
                            <p class="text-gray-600 mb-2"><%= status.text %></p>
                            <time class="text-sm text-gray-400">
                                <%= new Date(status.created_at).toLocaleDateString('en-US', { 
                                    weekday: 'long',
                                    year: 'numeric', 
                                    month: 'long', 
                                    day: 'numeric',
                                    hour: '2-digit',
                                    minute: '2-digit'
                                }) %>
                            </time>
                        </div>
                    <% }) %>
                </div>

                <% if (statuses.length === 0) { %>
                    <p class="text-center text-gray-500">No status updates yet.</p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html> 